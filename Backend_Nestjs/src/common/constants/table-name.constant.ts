// PROJECT PREFIX
export const PROJECT_PREFIX = "table_";

// TABLE NAMES
export const TABLE_NAMES = {
  USER: `${PROJECT_PREFIX}user`,
  EVENT: `${PROJECT_PREFIX}event`,
  RSVP: `${PROJECT_PREFIX}rsvp`,
};
